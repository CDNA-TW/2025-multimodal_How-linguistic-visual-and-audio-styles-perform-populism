# Python dependencies for this repository, compiled from the import
# statements actually used in code/ (see README.md for which folder needs
# which subset). Versions below are pinned where a genuine `pip freeze`
# snapshot from one of the original project environments could be found
# (see the per-line comments); everything else is left unpinned since no
# recorded version exists -- before publishing, run
# `pip freeze > requirements-lock.txt` yourself and ship that alongside
# this file for full reproducibility.
#
# torch: install the build matching your CUDA version first following
# https://pytorch.org/get-started/locally/ , then install the rest of this
# file (some packages below depend on torch already being present).
#
# face_recognition: requires dlib, which needs CMake and a C++ compiler to
# build from source; see https://github.com/ageitgey/face_recognition#installation
#
# face-alignment (used by visual_valence_arousal_loop.py for its SFD face
# detector) also needs pretrained EmoNet weights that are NOT included in
# this repo -- see the docstring at the top of that script.

# Data crawling
instaloader
emoji
openpyxl

# Core data / numerics
numpy==1.24.3
pandas==2.0.3
scipy==1.10.1
scikit-learn==1.3.2

# Plotting
matplotlib==3.7.5
seaborn==0.13.2

# Deep learning / NLP
torch==2.4.1
transformers==4.46.2
datasets==3.1.0
sentence-transformers
huggingface_hub==0.26.2

# Audio processing
demucs==4.0.1
pyannote.audio==3.3.0
librosa==0.10.2.post1
pydub==0.25.1
openai-whisper==20240930
nltk

# Pitch/formants (Praat via Parselmouth), duration/VUV (OpenSMILE), and
# MFCC0 (SpeechBrain) each had an extraction step in the paper (Online
# Appendix I) but no script for any of them could be recovered from the
# team's records -- only the resulting feature CSVs are consumed downstream
# (see README's audio processing execution-order note). Neither package is
# actually imported by any script in this repo, so both are commented out
# here; the pinned versions below are confirmed from a genuine environment
# snapshot found in the team's records -- uncomment (and add parselmouth)
# only if/when you write a replacement extractor.
# opensmile==2.5.0
# speechbrain==1.0.2

# Video / facial feature extraction
opencv-python
mediapipe
face_recognition
face-alignment
scikit-image
py-feat

# Text / topic modeling
bertopic
umap-learn
kneed
liwc  # tokenizer/parser only -- requires a separately licensed
      # LIWC2022_English.dic from https://www.liwc.app/, not redistributable here

# LLM-based labeling / portrait generation (optional, calls OpenAI API)
openai

# Misc
tqdm==4.67.0
